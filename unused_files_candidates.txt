Lista de ficheros candidatos a NO USO (generada automáticamente)

Instrucciones rápidas:
- Esta es una lista inicial basada en búsqueda de referencias en el código (coincidencias de nombre). No borres nada sin comprobarlo manualmente.
- He excluido la carpeta "vendor/" de este análisis automáticamente.
- Para cada fichero indico: ruta, motivo (por qué parece no usarse), nivel de confianza y recomendación.

Formato: <ruta> — <motivo> — <confianza> — <recomendación>

1) ./config.php — Ninguna referencia a este fichero desde el código (no aparece en includes ni en formularios). Podría ser un fichero de configuración local/manual. — Alta — Revisar su contenido; si no lo usas, mover a backups y eliminar. Si contiene credenciales, extráelo a .env y elimina del repo.

2) ./old.dashboard.php — Solo apariciones dentro del propio fichero; no hay enlaces ni inclusiones desde otras partes. Parece una versión antigua del dashboard. — Alta — Mantener copia de seguridad y eliminar si confirmas que el nuevo `dashboard.php` cubre todo.

3) ./ftch.php — Solo aparece su propia cabecera; ningún otro fichero lo referencia. Posible script antiguo para listar transacciones. — Alta — Revisar su funcionalidad y migrarla o eliminar.

4) ./insert_categoria.php — Ninguna referencia externa detectada. Puede ser un endpoint antiguo no usado por los formularios actuales. — Alta — Verificar formularios / action en vistas; si no se usa, eliminar.

5) ./insert.php — No se encuentra referencia por nombre desde otras partes del proyecto. — Alta — Revisar si es un endpoint obsoleto; eliminar o archivar.

6) ./gestion_categorias.php — Solo referencias internas (redirects al propio archivo). La navegación actual apunta a `views/gestion_conceptos.php` en la cabecera, así que esta versión parece obsoleta. — Alta — Comparar con `views/gestion_conceptos.php` y eliminar si es duplicado/antiguo.

7) ./token_github/token — No hay referencias y además es un fichero que parece contener un token (semejante a credencial). Esto es potencialmente SECRETO y no debería estar en el repo. — Muy alta (por seguridad) — Mover/deletar inmediatamente y rotar el token (revocar y generar uno nuevo). Añadir a .gitignore.

8) ./sql/crear_subcategorias.sql — No referencias en código (es script de migración/manual). — Media — Mantener como script de DB si lo necesitas; archivarlo en `sql/migrations/` o README si no.

9) ./sql/estructura_categorias_fijas.sql — Script de estructura; no referenciado por la app. — Media — Mantener en SQL/migrations si lo necesitas, o archivar.

Observaciones generales y pasos siguientes recomendados:
- Confianza: "Alta" significa que la búsqueda no encontró ninguna referencia con el nombre del fichero en el código fuente. "Media" para scripts SQL porque suelen ser archivos de referencia. Esto no garantiza que un fichero no se use indirectamente (por ejemplo via include variable, rutas dinámicas, ejecuciones manuales, cron, o llamadas externas).
- Antes de eliminar: 1) haz copia de seguridad (zip/commit) 2) busca usos dinámicos (include/require con variables, rutas construidas en runtime, llamadas HTTP externas). Puedo ejecutar una búsqueda adicional por patrones dinámicos (e.g., include\s*\(|require\s*\(|file_get_contents\(|fopen\() si quieres.
- Seguridad: `token_github/token` debe tratarse inmediatamente si es realmente un secreto. Recomiendo rotarlo y eliminarlo del repositorio.

¿Quieres que:
A) archive estos ficheros (mover a ./archivado_no_usado/) y añadirlos a .gitignore? (puedo aplicar los cambios)
B) haga una búsqueda ampliada para detectar usos dinámicos e incluya un informe más exhaustivo? (recomendado antes de borrar)
C) ejecute un script que intente abrir cada archivo por nombre desde el entorno web para confirmar 404/200? (requiere entorno web activo)

---
Generado automáticamente. Si confirmas, puedo aplicar cambios (mover/archivar) o continuar con la auditoría dinámica.
